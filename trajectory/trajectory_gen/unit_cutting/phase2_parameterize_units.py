import argparse
import json
import os
import re
import shlex
import subprocess
import time
from datetime import datetime
from pathlib import Path
from typing import Any, Dict, Iterable, List, Optional, Sequence, Tuple


LOG_FILE_PATH: Optional[Path] = None

IGNORED_SESSION_DIR_NAMES = {
    "bak",
    "backup",
    "backups",
    "splits",
    "_debug",
    "__pycache__",
    "segments_units_phase2",
}


SYSTEM_PROMPT = """You are an expert at abstracting concrete GUI interaction units into reusable parameterized units.

Your task is to transform one already-segmented concrete unit into one parameterized unit.

A concrete unit already has a clear semantic meaning. Your job is NOT to re-segment it.
Your job is to:
1. Abstract the unit intent into a reusable sub-goal.
2. Identify instance-specific values that should become parameters.
3. Convert the concrete interaction into a parameterized action sequence.

A good parameterized unit should satisfy the following principles:

[Abstraction Principles]
- Preserve stable semantics:
  Keep the invariant functional meaning of the unit.
  Do not over-abstract it into vague descriptions like "perform an operation" or "modify something".

- Extract only reusable variable parts:
  Only values that vary across instances but play stable semantic roles should become parameters.
  Examples include:
  target_element, target_field, input_text, color_value, target_page, folder_name, file_name, option_name

- Do not parameterize everything:
  Stable operational concepts should remain in the abstract description.
  For example, "open text color dialog" should usually remain a stable operation, while the concrete color value may become a parameter.

- Abstract intent must be sub-goal level:
  abstract_intent should describe the reusable purpose of the unit, not low-level motor actions.
  Good: "Apply a specified custom text color to the selected text object."
  Bad: "Click the color button and type a hex value."

- Parameter values must be grounded:
  Every parameter value must be supported by the concrete unit annotation or the raw steps.

- Parameterized action sequence must stay executable:
  Each action template should still describe a concrete reusable interaction step, with placeholders only where necessary.

[Output Constraints]
You must output exactly one parameterized unit for the given concrete unit.
Do not change the original step_indices.
Do not omit required fields.
Return only valid JSON.
Do NOT output markdown.
Do NOT output explanations outside the JSON.

[Field Requirements]
The output JSON must contain:
- param_unit_id
- unit_id
- step_indices
- unit_intent
- unit_type
- unit_before_state
- unit_after_state
- unit_precondition
- unit_effect
- phase1_status
- abstract_intent
- parameters
- parameterized_action_sequence
- phase2_status

Definitions:
- abstract_intent:
  A reusable, abstract sub-goal derived from the concrete unit.
  It should remove instance-specific details such as concrete slide number, file name, exact literal text, exact page name, etc., unless they are essential and truly stable.

- parameters:
  A JSON object mapping parameter names to concrete bound values.
  Example:
  {
    "target_element": "slide 1 title text",
    "color_value": "#5F77BB"
  }

- parameterized_action_sequence:
  A list of structured action templates.
  Each item must contain:
  - action: Directly use the `action` field from the original steps; there is no need to define a new one, such as "press ctrl a"/"click left"/"double_click".
  - action_template: Generated by combining the four fields from the current step: `nl_explanation`, `action`, `action_preconditions`, and `action_after_effects`, and extract parameters.

Example:
[
  {
    "action": "press Ctrl+C",
    "action_template": "Press Ctrl+C to copy the selected hex color code."
  },
  {
    "action": "click",
    "action_template": "Click {{confirm_button}} to close the color dialog after copying the color value."
  },
  {
    "action": "typing",
    "action_template": "Type {{replacement_text}} to replace the old title."
  }
]

[Important Validation-Oriented Rules]
- Every placeholder appearing in parameterized_action_sequence must have a matching key in parameters.
- Do not create parameters that are never used unless they are clearly necessary for abstract_intent grounding.
- abstract_intent should not still contain obvious instance-specific values like:
  "slide 1", "slide 2", exact file names, exact literal replacement text, exact folder name,
  unless the value is required and intentionally preserved as a parameterized role.
- If you keep a value as a parameter, do not repeat that literal value in abstract_intent.
  Use a neutral phrase or the placeholder form such as {{target_page}} instead.
- phase2_status should be "done" if the abstraction is valid.

IMPORTANT: Your output must be in ENGLISH.
"""


USER_PROMPT_TEMPLATE = """Please convert the following concrete unit into one parameterized unit.

Input assumptions:
- The unit has already been segmented correctly in Phase 1.
- You must not re-segment or merge anything.
- You must preserve the original concrete fields while adding the abstracted fields.
- The result will later be appended into the "parameterized units" field of the segment JSON.

Output schema:
Return only one JSON object with this schema:

{
  "param_unit_id": "pu_seg_001_01",
  "unit_id": "u_seg_001_01",
  "step_indices": [1, 2, 3],
  "unit_intent": "...",
  "unit_type": "...",
  "unit_before_state": "...",
  "unit_after_state": "...",
  "unit_precondition": ["..."],
  "unit_effect": ["..."],
  "phase1_status": "done",
  "abstract_intent": "...",
  "parameters": {
    "param_name": "param_value"
  },
  "parameterized_action_sequence": [
    {
      "action": "...",
      "action_template": "..."
    }
  ],
  "phase2_status": "done"
}

Additional rules:
- param_unit_id should be derived from unit_id by replacing the "u_" prefix with "pu_".
- Except for the new JSON fields: "param_unit_id", "phase2_status", "abstract_intent", "parameters" and "parameterized_action_sequence", simply copy and paste the contents of the corresponding fields from given concrete unit.
- Keep all concrete unit fields unchanged.
- abstract_intent must be more reusable than unit_intent.
- parameters should include only instance-specific values that are useful for reuse.
- parameterized_action_sequence should describe reusable interaction steps with placeholders such as:
  {{target_element}}, {{input_text}}, {{color_value}}, {{target_page}}, {{folder_name}}
- If a concrete value appears in abstract_intent or action templates and should vary across instances, convert it into a parameter.
- If a value is already in parameters, do not repeat the literal value in abstract_intent.
  Either use the placeholder form there or rewrite abstract_intent in neutral wording.

Now process this concrete unit input and output only the parameterized unit JSON:

<PHASE2_INPUT>
{phase2_input_here}
</PHASE2_INPUT>"""


LOW_SIGNAL_INTENTS = (
    "click the button",
    "click button",
    "press enter",
    "press the button",
    "type text",
    "input text",
    "enter text",
    "click the text box",
    "click the input field",
    "perform an operation",
    "modify something",
)

PLACEHOLDER_PATTERN = re.compile(r"\{\{\s*([a-zA-Z_][a-zA-Z0-9_]*)\s*\}\}")
INSTANCE_SPECIFIC_PATTERNS = (
    re.compile(r"\bslide\s+\d+\b", re.IGNORECASE),
    re.compile(r"\b\S+\.(pptx|ppt|docx|doc|xlsx|xls|odp|odt|pdf)\b", re.IGNORECASE),
)
NUMBER_WORDS = {
    0: "zero",
    1: "one",
    2: "two",
    3: "three",
    4: "four",
    5: "five",
    6: "six",
    7: "seven",
    8: "eight",
    9: "nine",
    10: "ten",
    11: "eleven",
    12: "twelve",
}
TEXT_NORMALIZATION_TABLE = str.maketrans(
    {
        "“": '"',
        "”": '"',
        "‘": "'",
        "’": "'",
        "\u00a0": " ",
    }
)


def format_timestamp(dt: datetime) -> str:
    return dt.strftime("%Y-%m-%d %H:%M:%S")


def format_duration(seconds: float) -> str:
    if seconds < 60:
        return f"{seconds:.1f}s"
    minutes, remaining_seconds = divmod(seconds, 60)
    if minutes < 60:
        return f"{int(minutes)}m {remaining_seconds:.1f}s"
    hours, remaining_minutes = divmod(minutes, 60)
    return f"{int(hours)}h {int(remaining_minutes)}m {remaining_seconds:.1f}s"


def log_with_timestamp(message: str) -> None:
    line = f"[{format_timestamp(datetime.now())}] {message}"
    print(line, flush=True)
    if LOG_FILE_PATH is not None:
        LOG_FILE_PATH.parent.mkdir(parents=True, exist_ok=True)
        with LOG_FILE_PATH.open("a", encoding="utf-8") as f:
            f.write(line + "\n")


def configure_log_file(log_file: str) -> None:
    global LOG_FILE_PATH
    LOG_FILE_PATH = Path(log_file).resolve() if log_file else None


def load_env_file(env_path: Path) -> None:
    if not env_path.is_file():
        return

    for raw_line in env_path.read_text(encoding="utf-8").splitlines():
        line = raw_line.strip()
        if not line or line.startswith("#") or "=" not in line:
            continue
        key, value = line.split("=", 1)
        key = key.strip()
        value = value.strip().strip("'").strip('"')
        if key and key not in os.environ:
            os.environ[key] = value


def natural_sort_key(path: Path) -> Tuple[int, str]:
    match = re.search(r"(\d+)$", path.stem)
    if match:
        return (int(match.group(1)), path.name.lower())
    return (10**9, path.name.lower())


def normalize_units_subdir(units_subdir: str) -> Path:
    normalized = Path(units_subdir.strip().strip("/\\"))
    if not normalized.parts:
        raise ValueError("--units-subdir must not be empty")
    if normalized.is_absolute():
        raise ValueError("--units-subdir must be a relative subdirectory path")
    return normalized


def resolve_session_paths(input_path: Path, units_subdir: Path) -> Tuple[Path, Path]:
    if input_path.is_dir() and input_path.name == units_subdir.name:
        session_dir = input_path
        for _ in units_subdir.parts:
            session_dir = session_dir.parent
        candidate_dir = session_dir / units_subdir
        if candidate_dir == input_path:
            return session_dir, candidate_dir
    if input_path.is_dir() and (input_path / units_subdir).is_dir():
        return input_path, input_path / units_subdir
    raise FileNotFoundError(
        f"Could not find a session directory with {units_subdir.as_posix()} under: {input_path}"
    )


def discover_session_dirs(input_path: Path, units_subdir: Path) -> List[Path]:
    try:
        session_dir, _ = resolve_session_paths(input_path, units_subdir)
        return [session_dir]
    except FileNotFoundError:
        pass

    if not input_path.is_dir():
        raise FileNotFoundError(
            f"Could not find a session directory or batch root under: {input_path}"
        )

    session_dirs = []
    for units_dir in input_path.rglob(units_subdir.name):
        if not units_dir.is_dir():
            continue
        relative_parts = units_dir.relative_to(input_path).parts
        if relative_parts[-len(units_subdir.parts):] != units_subdir.parts:
            continue
        session_dir = units_dir
        for _ in units_subdir.parts:
            session_dir = session_dir.parent
        session_dirs.append(session_dir)
    session_dirs = sorted(set(session_dirs), key=lambda path: str(path).lower())
    if not session_dirs:
        raise FileNotFoundError(
            f"No session directories containing {units_subdir.as_posix()} were found under: {input_path}"
        )
    return session_dirs


def discover_batch_session_dirs(input_path: Path, units_subdir: Path, recursive: bool) -> List[Path]:
    if not input_path.is_dir():
        raise FileNotFoundError(f"Batch root is not a directory: {input_path}")

    if recursive:
        session_dirs = []
        for units_dir in input_path.rglob(units_subdir.name):
            if not units_dir.is_dir():
                continue
            relative_parts = units_dir.relative_to(input_path).parts
            if relative_parts[-len(units_subdir.parts):] != units_subdir.parts:
                continue
            session_dir = units_dir
            for _ in units_subdir.parts:
                session_dir = session_dir.parent
            if session_dir.name.lower() in IGNORED_SESSION_DIR_NAMES:
                continue
            session_dirs.append(session_dir)
        session_dirs = sorted(set(session_dirs), key=lambda path: str(path).lower())
    else:
        session_dirs = sorted(
            [
                child
                for child in input_path.iterdir()
                if child.is_dir()
                and child.name.lower() not in IGNORED_SESSION_DIR_NAMES
                and (child / units_subdir).is_dir()
            ],
            key=lambda path: str(path).lower(),
        )

    if not session_dirs:
        search_mode = "recursively" if recursive else "under direct children"
        raise FileNotFoundError(
            f"No session directories containing {units_subdir.as_posix()} were found {search_mode}: {input_path}"
        )
    return session_dirs


def read_json_file(json_path: Path) -> Dict[str, Any]:
    if not json_path.is_file():
        return {}
    try:
        data = json.loads(json_path.read_text(encoding="utf-8"))
    except Exception:
        return {}
    return data if isinstance(data, dict) else {}


def write_status_file(status_path: Path, payload: Dict[str, Any]) -> None:
    status_path.write_text(
        json.dumps(payload, ensure_ascii=False, indent=2),
        encoding="utf-8",
    )


def status_file_path_for_session(session_dir: Path) -> Path:
    return session_dir / "_phase2_unit_parameterization_status.json"


def resolve_output_dir_for_session(
    session_dir: Path,
    explicit_output_dir: str,
    batch_mode: bool,
) -> Path:
    if not explicit_output_dir:
        return session_dir / "segments_units_phase2"

    resolved = Path(explicit_output_dir).resolve()
    if batch_mode:
        return resolved / session_dir.name / "segments_units_phase2"
    return resolved


def choose_unit_before_image(
    steps: Sequence[Dict[str, Any]],
    step_indices: Sequence[int],
    session_dir: Path,
) -> str:
    if not step_indices:
        return ""
    first_step = steps[step_indices[0] - 1]
    now_state = first_step.get("now_state") or {}
    for rel_path in (
        now_state.get("screenshot_path_before_part"),
        now_state.get("screenshot_path_before"),
    ):
        if rel_path and (session_dir / str(rel_path)).is_file():
            return str(rel_path)
    return str(now_state.get("screenshot_path_before") or "")


def choose_unit_after_image(
    steps: Sequence[Dict[str, Any]],
    step_indices: Sequence[int],
    session_dir: Path,
) -> str:
    if not step_indices:
        return ""
    last_step = steps[step_indices[-1] - 1]
    now_state = last_step.get("now_state") or {}
    rel_path = now_state.get("screenshot_path_after")
    if rel_path and (session_dir / str(rel_path)).is_file():
        return str(rel_path)
    return str(rel_path or "")


def normalize_target_description(target: Any) -> str:
    if not isinstance(target, dict):
        return ""

    target_text = target.get("text")
    if isinstance(target_text, str) and target_text.strip():
        return target_text.strip()

    nl_position = target.get("nl_position")
    if isinstance(nl_position, list):
        return "; ".join(str(item) for item in nl_position if str(item).strip())
    if isinstance(nl_position, str):
        return nl_position.strip()

    describe = target.get("describe")
    if isinstance(describe, dict):
        return json.dumps(describe, ensure_ascii=False)
    if isinstance(describe, str):
        return describe.strip()
    return ""


def build_action_parameters(action: Dict[str, Any]) -> Dict[str, Any]:
    params = action.get("param")
    if isinstance(params, dict) and params:
        return params

    compact: Dict[str, Any] = {}
    for key in ("press_list", "button", "num_click", "num_clicks", "text"):
        value = action.get(key)
        if value not in (None, "", [], {}):
            compact[key] = value
    return compact


def compact_step_for_prompt(step: Dict[str, Any], step_index: int) -> Dict[str, Any]:
    action = step.get("action")
    now_state = step.get("now_state") or {}
    return {
        "step_index": step_index,
        "step_id": step.get("step_id"),
        "step_goal": step.get("step_goal"),
        "app_title_before": now_state.get("app_title_before"),
        "app_title_after": now_state.get("app_title_after"),
        "action": action if isinstance(action, dict) else action,
        "action_preconditions": step.get("action_preconditions") or [],
        "action_before_state": step.get("action_before_state"),
        "action_after_effects": step.get("action_after_effects") or [],
        "nl_explanation": step.get("nl_explanation"),
    }


def build_phase2_input(
    segment_data: Dict[str, Any],
    concrete_unit: Dict[str, Any],
    session_dir: Path,
) -> Dict[str, Any]:
    steps = segment_data.get("steps") or []
    step_indices = concrete_unit.get("step_indices") or []
    indexed_steps = [
        compact_step_for_prompt(steps[idx - 1], idx)
        for idx in step_indices
        if 1 <= idx <= len(steps)
    ]
    return {
        "segment_id": segment_data.get("segment_id"),
        "app": segment_data.get("app"),
        "env": segment_data.get("env") or {},
        "concrete_unit": {
            "unit_id": concrete_unit.get("unit_id"),
            "step_indices": concrete_unit.get("step_indices") or [],
            "unit_intent": concrete_unit.get("unit_intent"),
            "unit_type": concrete_unit.get("unit_type"),
            "unit_before_state": concrete_unit.get("unit_before_state"),
            "unit_after_state": concrete_unit.get("unit_after_state"),
            "unit_precondition": concrete_unit.get("unit_precondition") or [],
            "unit_effect": concrete_unit.get("unit_effect") or [],
            "phase1_status": concrete_unit.get("phase1_status"),
        },
        "unit_steps": indexed_steps,
        "unit_screenshots": {
            "before": choose_unit_before_image(steps, step_indices, session_dir),
            "after": choose_unit_after_image(steps, step_indices, session_dir),
        },
    }


def build_codex_user_prompt(
    phase2_input: Dict[str, Any],
    source_path: Path,
    session_dir: Path,
) -> str:
    prompt_text = USER_PROMPT_TEMPLATE.replace(
        "{phase2_input_here}",
        json.dumps(phase2_input, ensure_ascii=False, indent=2),
    )
    return (
        f"The source segment JSON file is located at:\n{source_path}\n\n"
        f"The current session directory is located at:\n{session_dir}\n\n"
        "You may inspect the local files directly before answering, especially the before/after screenshots listed in unit_screenshots when they help abstraction.\n\n"
        f"{prompt_text}"
    )


def _find_codex_path() -> str:
    return "codex"


def call_codex(model: str, system_prompt: str, user_prompt: str) -> str:
    import tempfile

    prompt = f"{system_prompt.rstrip()}\n\n{user_prompt}".strip()

    with tempfile.NamedTemporaryFile(
        mode="w",
        suffix=".txt",
        encoding="utf-8",
        delete=False,
    ) as file:
        prompt_file = file.name
        file.write(prompt)

    try:
        codex_path = _find_codex_path()
        # base = f'"{codex_path}" exec --skip-git-repo-check -c reasoning_effort=medium'
        # 开启fast_mode
        base = f'"{codex_path}" exec --skip-git-repo-check -c features.fast_mode=true -c service_tier=fast -c reasoning_effort=medium'
        if model:
            base += f" --model {shlex.quote(model)}"
        base += " --json"

        if os.name == "nt":
            cmd = f'cmd /c "{base} < {prompt_file}"'
            result = subprocess.run(
                cmd,
                shell=True,
                capture_output=True,
                text=True,
                encoding="utf-8",
                errors="replace",
            )
        else:
            cmd = shlex.split(base)
            with open(prompt_file, "r", encoding="utf-8") as stdin_file:
                result = subprocess.run(
                    cmd,
                    stdin=stdin_file,
                    capture_output=True,
                    text=True,
                    encoding="utf-8",
                    errors="replace",
                )
    finally:
        os.unlink(prompt_file)

    if result.returncode != 0:
        stderr_msg = result.stderr if result.stderr else result.stdout
        raise RuntimeError(f"Codex error: {stderr_msg}")

    last_content: Optional[str] = None
    for line in result.stdout.splitlines():
        line = line.strip()
        if not line:
            continue
        try:
            event = json.loads(line)
        except Exception:
            continue

        if event.get("type") in ("item.started", "item.completed"):
            item = event.get("item", {})
            item_type = item.get("type") or item.get("item_type")
            if item_type in ("assistant_message", "agent_message"):
                content = item.get("text") or item.get("content")
                if isinstance(content, list):
                    content = "".join(
                        part if isinstance(part, str) else part.get("text", "")
                        for part in content
                    )
                last_content = content

    if last_content is not None:
        return last_content
    return "(no assistant message returned)"


def extract_json_text(raw_text: str) -> str:
    text = raw_text.strip()
    if text.startswith("```"):
        parts = text.split("```")
        for part in parts:
            candidate = part.strip()
            if candidate.startswith("json"):
                candidate = candidate[4:].strip()
            if "{" in candidate and "}" in candidate:
                text = candidate
                break

    start = text.find("{")
    end = text.rfind("}")
    if start != -1 and end != -1 and end > start:
        return text[start : end + 1]
    return text


def parse_response_json(raw_text: str) -> Dict[str, Any]:
    return json.loads(extract_json_text(raw_text))


def normalize_text_list(value: Any) -> List[str]:
    if isinstance(value, list):
        return [str(item).strip() for item in value if str(item).strip()]
    if isinstance(value, str) and value.strip():
        return [value.strip()]
    return []


def normalize_step_indices(step_indices: Any) -> List[int]:
    normalized: List[int] = []
    if not isinstance(step_indices, list):
        return normalized
    for item in step_indices:
        if isinstance(item, int):
            normalized.append(item)
            continue
        if isinstance(item, str):
            stripped = item.strip().lower()
            if stripped.startswith("s") and stripped[1:].isdigit():
                normalized.append(int(stripped[1:]))
            elif stripped.isdigit():
                normalized.append(int(stripped))
    return normalized


def derive_param_unit_id(unit_id: str) -> str:
    if unit_id.startswith("u_"):
        return f"pu_{unit_id[2:]}"
    return f"pu_{unit_id}"


def normalize_parameters(value: Any) -> Dict[str, Any]:
    if isinstance(value, dict):
        return {str(key).strip(): item for key, item in value.items() if str(key).strip()}
    return {}


def replace_parameter_values_with_placeholders(
    text: str,
    parameters: Dict[str, Any],
) -> str:
    normalized_text = str(text or "").strip()
    if not normalized_text or not parameters:
        return normalized_text

    for param_name, param_value in parameters.items():
        placeholder = f"{{{{{param_name}}}}}"
        if placeholder in normalized_text:
            continue
        if not isinstance(param_value, (str, int, float, bool)):
            continue
        value_text = str(param_value).strip()
        if len(value_text) < 4:
            continue
        if value_text in normalized_text:
            normalized_text = normalized_text.replace(value_text, placeholder)

    return normalized_text


def normalize_parameterized_action_sequence(value: Any) -> List[Dict[str, str]]:
    if not isinstance(value, list):
        return []
    normalized: List[Dict[str, str]] = []
    for item in value:
        if not isinstance(item, dict):
            continue
        normalized.append(
            {
                "action": str(item.get("action", "")).strip(),
                "action_template": str(item.get("action_template", "")).strip(),
            }
        )
    return normalized


def normalize_parameterized_unit(
    raw_unit: Dict[str, Any],
    concrete_unit: Dict[str, Any],
) -> Dict[str, Any]:
    parameters = normalize_parameters(raw_unit.get("parameters"))
    abstract_intent = replace_parameter_values_with_placeholders(
        str(raw_unit.get("abstract_intent", raw_unit.get("abstract_intend", ""))).strip(),
        parameters,
    )
    return {
        "param_unit_id": str(raw_unit.get("param_unit_id", "")).strip()
        or derive_param_unit_id(str(concrete_unit.get("unit_id", "")).strip()),
        "unit_id": concrete_unit.get("unit_id"),
        "step_indices": concrete_unit.get("step_indices") or [],
        "unit_intent": concrete_unit.get("unit_intent"),
        "unit_type": concrete_unit.get("unit_type"),
        "unit_before_state": concrete_unit.get("unit_before_state"),
        "unit_after_state": concrete_unit.get("unit_after_state"),
        "unit_precondition": concrete_unit.get("unit_precondition") or [],
        "unit_effect": concrete_unit.get("unit_effect") or [],
        "phase1_status": concrete_unit.get("phase1_status"),
        "abstract_intent": abstract_intent,
        "parameters": parameters,
        "parameterized_action_sequence": normalize_parameterized_action_sequence(
            raw_unit.get("parameterized_action_sequence")
        ),
        "phase2_status": str(raw_unit.get("phase2_status", "")).strip(),
        "phase2_error": str(raw_unit.get("phase2_error", "")).strip(),
    }


def is_low_signal_intent(intent: str) -> bool:
    normalized = re.sub(r"\s+", " ", intent.strip().lower())
    return any(phrase in normalized for phrase in LOW_SIGNAL_INTENTS)


def find_placeholders(text: str) -> List[str]:
    return PLACEHOLDER_PATTERN.findall(text or "")


def collect_parameter_usage(parameterized_unit: Dict[str, Any]) -> List[str]:
    usage: List[str] = []
    usage.extend(find_placeholders(str(parameterized_unit.get("abstract_intent", ""))))
    for item in parameterized_unit.get("parameterized_action_sequence") or []:
        usage.extend(find_placeholders(str(item.get("action_template", ""))))
    return usage


def build_grounding_text(
    concrete_unit: Dict[str, Any],
    unit_steps: Sequence[Dict[str, Any]],
) -> str:
    return json.dumps(
        {"concrete_unit": concrete_unit, "unit_steps": unit_steps},
        ensure_ascii=False,
    ).lower()


def normalize_line_breaks(value: Any) -> str:
    text = str(value or "").translate(TEXT_NORMALIZATION_TABLE)
    for escaped_newline in ("\\r\\n", "\\n", "\\r"):
        text = text.replace(escaped_newline, "\n")
    text = text.replace("\r\n", "\n").replace("\r", "\n")
    text = text.replace("\\t", " ").replace("\t", " ")
    return text


def normalize_match_text(value: Any) -> str:
    text = normalize_line_breaks(value).replace("\n", " ").strip().lower()
    return re.sub(r"\s+", " ", text)


def collect_grounding_fragments(value: Any) -> List[str]:
    fragments: List[str] = []

    def _walk(node: Any) -> None:
        if node is None:
            return
        if isinstance(node, str):
            normalized = normalize_match_text(node)
            if normalized:
                fragments.append(normalized)
            return
        if isinstance(node, dict):
            for child in node.values():
                _walk(child)
            return
        if isinstance(node, (list, tuple, set)):
            for child in node:
                _walk(child)
            return
        if isinstance(node, (int, float, bool)):
            normalized = normalize_match_text(node)
            if normalized:
                fragments.append(normalized)

    _walk(value)
    return fragments


def iter_parameter_leaf_values(value: Any) -> Iterable[str]:
    if value is None:
        return
    if isinstance(value, dict):
        for child in value.values():
            yield from iter_parameter_leaf_values(child)
        return
    if isinstance(value, (list, tuple, set)):
        for child in value:
            yield from iter_parameter_leaf_values(child)
        return
    if isinstance(value, (str, int, float, bool)):
        yield str(value)


def is_text_grounded(
    value_text: str,
    grounding_text: str,
    grounding_fragments: Sequence[str],
) -> bool:
    normalized_value = normalize_match_text(value_text)
    if not normalized_value:
        return False

    if any(normalized_value in fragment for fragment in grounding_fragments):
        return True
    if normalized_value in grounding_text:
        return True

    normalized_lines = [
        normalize_match_text(line)
        for line in normalize_line_breaks(value_text).splitlines()
    ]
    significant_lines = [line for line in normalized_lines if len(line) >= 3]
    if len(significant_lines) >= 2:
        return all(
            any(line in fragment for fragment in grounding_fragments) or line in grounding_text
            for line in significant_lines
        )

    return False


def iter_string_nodes(value: Any) -> Iterable[str]:
    if value is None:
        return
    if isinstance(value, str):
        yield value
        return
    if isinstance(value, dict):
        for child in value.values():
            yield from iter_string_nodes(child)
        return
    if isinstance(value, (list, tuple, set)):
        for child in value:
            yield from iter_string_nodes(child)


def is_count_parameter(param_name: str, param_value: Any) -> bool:
    if isinstance(param_value, bool):
        return False
    if isinstance(param_value, int):
        return True
    if isinstance(param_value, float) and float(param_value).is_integer():
        return True
    normalized_name = str(param_name or "").strip().lower()
    return normalized_name.endswith("_count") or normalized_name == "count"


def count_tab_tokens(text: str) -> int:
    return text.count("\t") + text.count("\\t")


def count_parameter_keywords(param_name: str) -> List[str]:
    normalized_name = str(param_name or "").strip().lower()
    if normalized_name == "tab_count":
        return ["tab", "tabs", "tab move", "tab moves"]
    base = re.sub(r"_count$", "", normalized_name)
    if not base:
        return []
    spaced = base.replace("_", " ").strip()
    keywords = [spaced]
    if not spaced.endswith("s"):
        keywords.append(f"{spaced}s")
    return [keyword for keyword in keywords if keyword]


def is_count_grounded(param_name: str, param_value: Any, grounding_source: Any) -> bool:
    if isinstance(param_value, bool):
        return False
    try:
        numeric_value = int(param_value)
    except (TypeError, ValueError):
        return False

    source_strings = list(iter_string_nodes(grounding_source))
    if not source_strings:
        return False

    if str(param_name).strip().lower() == "tab_count":
        if any(count_tab_tokens(text) == numeric_value for text in source_strings):
            return True

    word_variant = NUMBER_WORDS.get(numeric_value, "")
    value_variants = [str(numeric_value)]
    if word_variant:
        value_variants.append(word_variant)

    keywords = count_parameter_keywords(param_name)
    if not keywords:
        return False

    for text in source_strings:
        normalized_text = normalize_match_text(text)
        if not normalized_text:
            continue
        for value_variant in value_variants:
            for keyword in keywords:
                pattern = rf"\b{re.escape(value_variant)}\s+{re.escape(keyword)}\b"
                if re.search(pattern, normalized_text):
                    return True

    return False


def find_instance_specific_values(
    abstract_intent: str,
    parameters: Dict[str, Any],
) -> List[str]:
    findings: List[str] = []
    if not abstract_intent.strip():
        return findings
    for pattern in INSTANCE_SPECIFIC_PATTERNS:
        match = pattern.search(abstract_intent)
        if match:
            findings.append(match.group(0))
    for value in parameters.values():
        value_text = str(value).strip()
        if value_text and len(value_text) >= 4 and value_text in abstract_intent:
            findings.append(value_text)
    return list(dict.fromkeys(findings))


def validate_parameterized_unit(
    parameterized_unit: Dict[str, Any],
    concrete_unit: Dict[str, Any],
    unit_steps: Sequence[Dict[str, Any]],
) -> List[str]:
    errors: List[str] = []

    if parameterized_unit.get("param_unit_id") != derive_param_unit_id(
        str(concrete_unit.get("unit_id", ""))
    ):
        errors.append("param_unit_id does not match the required unit_id-derived format.")

    for field_name in (
        "unit_id",
        "unit_intent",
        "unit_type",
        "unit_before_state",
        "unit_after_state",
        "phase1_status",
    ):
        if parameterized_unit.get(field_name) != concrete_unit.get(field_name):
            errors.append(f"{field_name} was not preserved from the concrete unit.")

    if parameterized_unit.get("step_indices") != (concrete_unit.get("step_indices") or []):
        errors.append("step_indices were changed from the concrete unit.")
    if parameterized_unit.get("unit_precondition") != (concrete_unit.get("unit_precondition") or []):
        errors.append("unit_precondition was not preserved from the concrete unit.")
    if parameterized_unit.get("unit_effect") != (concrete_unit.get("unit_effect") or []):
        errors.append("unit_effect was not preserved from the concrete unit.")

    abstract_intent = str(parameterized_unit.get("abstract_intent", "")).strip()
    if not abstract_intent:
        errors.append("abstract_intent is missing.")
    elif len(abstract_intent) < 12:
        errors.append("abstract_intent is too short.")
    elif is_low_signal_intent(abstract_intent):
        errors.append("abstract_intent is too low-level or too vague.")

    parameters = parameterized_unit.get("parameters")
    if not isinstance(parameters, dict):
        errors.append("parameters must be a JSON object.")
        parameters = {}

    action_sequence = parameterized_unit.get("parameterized_action_sequence")
    if not isinstance(action_sequence, list) or not action_sequence:
        errors.append("parameterized_action_sequence must be a non-empty list.")
        action_sequence = []

    placeholders: List[str] = []
    for action_index, action_item in enumerate(action_sequence, start=1):
        if not isinstance(action_item, dict):
            errors.append(f"parameterized_action_sequence item {action_index} is not an object.")
            continue
        action_type = str(action_item.get("action", "")).strip()
        action_template = str(action_item.get("action_template", "")).strip()
        if not action_type:
            errors.append(f"parameterized_action_sequence item {action_index} is missing action.")
        if not action_template:
            errors.append(f"parameterized_action_sequence item {action_index} is missing action_template.")
        placeholders.extend(find_placeholders(action_template))

    missing_placeholders = sorted({name for name in placeholders if name not in parameters})
    if missing_placeholders:
        errors.append(
            "parameterized_action_sequence uses placeholders that are missing in parameters: "
            + ", ".join(missing_placeholders)
        )

    usage = collect_parameter_usage(parameterized_unit)
    unused_params = sorted({name for name in parameters if name not in usage})
    if unused_params:
        errors.append("parameters contains unused keys: " + ", ".join(unused_params))

    grounding_source = {"concrete_unit": concrete_unit, "unit_steps": unit_steps}
    grounding_text = normalize_match_text(build_grounding_text(concrete_unit, unit_steps))
    grounding_fragments = collect_grounding_fragments(grounding_source)
    for param_name, param_value in parameters.items():
        if is_count_parameter(param_name, param_value):
            if is_count_grounded(param_name, param_value, grounding_source):
                continue
        leaf_values = list(iter_parameter_leaf_values(param_value))
        if not leaf_values:
            errors.append(f"Parameter {param_name} has an empty value.")
            continue
        ungrounded_values = [
            value_text
            for value_text in leaf_values
            if not is_text_grounded(value_text, grounding_text, grounding_fragments)
        ]
        if ungrounded_values:
            errors.append(
                f"Parameter {param_name} value could not be grounded in the concrete unit or raw steps."
            )

    concrete_values_in_intent = find_instance_specific_values(abstract_intent, parameters)
    if concrete_values_in_intent:
        errors.append(
            "abstract_intent still appears to contain instance-specific concrete values: "
            + ", ".join(concrete_values_in_intent)
        )

    if str(parameterized_unit.get("phase2_status", "")).strip().lower() != "done":
        errors.append('phase2_status must be "done" for a valid output.')

    return errors


def build_output_payload(
    segment_data: Dict[str, Any],
    parameterized_units: List[Dict[str, Any]],
    phase2_error: str = "",
) -> Dict[str, Any]:
    payload: Dict[str, Any] = {
        "segment_id": segment_data.get("segment_id"),
        "app": segment_data.get("app"),
        "env": segment_data.get("env") or {},
        "steps": segment_data.get("steps") or [],
        "units": segment_data.get("units") or [],
        "parameterized units": parameterized_units,
    }
    if phase2_error:
        payload["phase2_error"] = phase2_error
    return payload


def build_failed_parameterized_unit(
    concrete_unit: Dict[str, Any],
    error_message: str,
) -> Dict[str, Any]:
    return {
        "param_unit_id": derive_param_unit_id(str(concrete_unit.get("unit_id", "")).strip()),
        "unit_id": concrete_unit.get("unit_id"),
        "step_indices": concrete_unit.get("step_indices") or [],
        "unit_intent": concrete_unit.get("unit_intent"),
        "unit_type": concrete_unit.get("unit_type"),
        "unit_before_state": concrete_unit.get("unit_before_state"),
        "unit_after_state": concrete_unit.get("unit_after_state"),
        "unit_precondition": concrete_unit.get("unit_precondition") or [],
        "unit_effect": concrete_unit.get("unit_effect") or [],
        "phase1_status": concrete_unit.get("phase1_status"),
        "abstract_intent": "",
        "parameters": {},
        "parameterized_action_sequence": [],
        "phase2_status": "failed",
        "phase2_error": error_message,
    }


def generate_validated_parameterized_unit(
    segment_data: Dict[str, Any],
    source_path: Path,
    concrete_unit: Dict[str, Any],
    session_dir: Path,
    debug_dir: Path,
    debug_prefix: str,
    model: str,
    max_retries: int,
    retry_delay: float,
) -> Tuple[bool, Dict[str, Any], str]:
    phase2_input = build_phase2_input(
        segment_data=segment_data,
        concrete_unit=concrete_unit,
        session_dir=session_dir,
    )
    base_user_prompt = build_codex_user_prompt(
        phase2_input=phase2_input,
        source_path=source_path,
        session_dir=session_dir,
    )
    unit_steps = phase2_input.get("unit_steps") or []

    last_error = ""
    for attempt in range(1, max_retries + 1):
        attempt_start = time.perf_counter()
        log_with_timestamp(f"{debug_prefix}: attempt {attempt}/{max_retries}")
        if attempt > 1 and last_error:
            correction_text = (
                "Previous output failed validation. Please regenerate the full JSON from scratch and fix these issues:\n"
                f"{last_error}\n"
                "Preserve all concrete fields exactly, ensure placeholders match parameters, and keep abstract_intent free of obvious instance-specific values. "
                "If a value is already stored in parameters, do not repeat that literal value in abstract_intent; use a placeholder there or rewrite the sentence in neutral wording."
            )
            attempt_user_prompt = f"{base_user_prompt}\n\n{correction_text}"
        else:
            attempt_user_prompt = base_user_prompt

        raw_output = ""
        try:
            raw_output = call_codex(model, SYSTEM_PROMPT, attempt_user_prompt)
            (debug_dir / f"{debug_prefix}_attempt_{attempt}_raw.txt").write_text(
                raw_output,
                encoding="utf-8",
            )
            parsed = parse_response_json(raw_output)
            parameterized_unit = normalize_parameterized_unit(parsed, concrete_unit)
            validation_errors = validate_parameterized_unit(
                parameterized_unit,
                concrete_unit=concrete_unit,
                unit_steps=unit_steps,
            )
            if validation_errors:
                last_error = "; ".join(validation_errors)
                log_with_timestamp(
                    f"{debug_prefix}: attempt {attempt} validation failed after "
                    f"{format_duration(time.perf_counter() - attempt_start)}: {last_error}"
                )
            else:
                parameterized_unit["phase2_error"] = ""
                log_with_timestamp(
                    f"{debug_prefix}: attempt {attempt} succeeded in "
                    f"{format_duration(time.perf_counter() - attempt_start)}"
                )
                return True, parameterized_unit, ""
        except Exception as exc:
            last_error = f"{type(exc).__name__}: {exc}"
            log_with_timestamp(
                f"{debug_prefix}: attempt {attempt} request failed after "
                f"{format_duration(time.perf_counter() - attempt_start)}: {last_error}"
            )
            if raw_output:
                (debug_dir / f"{debug_prefix}_attempt_{attempt}_raw.txt").write_text(
                    raw_output,
                    encoding="utf-8",
                )

        if attempt < max_retries:
            time.sleep(retry_delay)

    return False, build_failed_parameterized_unit(concrete_unit, last_error), last_error


def iter_segment_files(segments_units_dir: Path) -> Iterable[Path]:
    files = sorted(segments_units_dir.glob("*.json"), key=natural_sort_key)
    for file_path in files:
        if file_path.is_file():
            yield file_path


def load_existing_parameterized_units(output_path: Path) -> Dict[str, Dict[str, Any]]:
    if not output_path.is_file():
        return {}
    data = read_json_file(output_path)
    parameterized_units = data.get("parameterized units")
    if not isinstance(parameterized_units, list):
        return {}

    existing: Dict[str, Dict[str, Any]] = {}
    for item in parameterized_units:
        if not isinstance(item, dict):
            continue
        unit_id = str(item.get("unit_id", "")).strip()
        if unit_id:
            existing[unit_id] = item
    return existing


def is_reusable_done_parameterized_unit(
    existing_unit: Dict[str, Any],
    concrete_unit: Dict[str, Any],
) -> bool:
    if str(existing_unit.get("phase2_status", "")).strip().lower() != "done":
        return False
    if str(existing_unit.get("unit_id", "")).strip() != str(concrete_unit.get("unit_id", "")).strip():
        return False
    if existing_unit.get("step_indices") != (concrete_unit.get("step_indices") or []):
        return False
    return True


def parameterized_unit_success(unit: Dict[str, Any]) -> bool:
    return str(unit.get("phase2_status", "")).strip().lower() == "done"


def segment_file_success(payload: Dict[str, Any]) -> bool:
    parameterized_units = payload.get("parameterized units")
    if not isinstance(parameterized_units, list) or not parameterized_units:
        return False
    return all(
        isinstance(unit, dict) and parameterized_unit_success(unit)
        for unit in parameterized_units
    )


def count_completed_segment_outputs(
    segments_units_dir: Path,
    output_dir: Path,
    segment_limit: int,
) -> Tuple[int, int]:
    segment_files = list(iter_segment_files(segments_units_dir))
    if segment_limit > 0:
        segment_files = segment_files[:segment_limit]

    completed_count = 0
    for source_path in segment_files:
        output_path = output_dir / source_path.name
        if segment_file_success(read_json_file(output_path)):
            completed_count += 1
    return completed_count, len(segment_files)


def session_outputs_completed(
    segments_units_dir: Path,
    output_dir: Path,
    segment_limit: int,
) -> bool:
    completed_count, total_count = count_completed_segment_outputs(
        segments_units_dir=segments_units_dir,
        output_dir=output_dir,
        segment_limit=segment_limit,
    )
    return total_count > 0 and completed_count == total_count


def process_segment_file(
    source_path: Path,
    session_dir: Path,
    output_dir: Path,
    debug_dir: Path,
    model: str,
    max_retries: int,
    retry_delay: float,
    force: bool,
) -> Tuple[bool, str, int, int]:
    segment_wall_start = datetime.now()
    segment_perf_start = time.perf_counter()
    segment_data = json.loads(source_path.read_text(encoding="utf-8"))
    segment_id = str(segment_data.get("segment_id") or source_path.stem)
    units = segment_data.get("units") or []

    log_with_timestamp(
        f"{segment_id}: start parameterization for {source_path.name} "
        f"with {len(units) if isinstance(units, list) else 0} units"
    )

    if not isinstance(units, list) or not units:
        payload = build_output_payload(
            segment_data=segment_data,
            parameterized_units=[],
            phase2_error="Source segment JSON has no concrete units.",
        )
        (output_dir / source_path.name).write_text(
            json.dumps(payload, ensure_ascii=False, indent=2),
            encoding="utf-8",
        )
        log_with_timestamp(
            f"{segment_id}: failed in {format_duration(time.perf_counter() - segment_perf_start)} "
            f"(no concrete units)"
        )
        return False, "Source segment JSON has no concrete units.", 0, 0

    output_path = output_dir / source_path.name
    if output_path.is_file() and not force:
        existing_payload = read_json_file(output_path)
        if segment_file_success(existing_payload):
            parameterized_units = existing_payload.get("parameterized units") or []
            unit_success_count = sum(
                1 for unit in parameterized_units if isinstance(unit, dict) and parameterized_unit_success(unit)
            )
            log_with_timestamp(f"{segment_id}: reuse existing successful segment output")
            return True, "", unit_success_count, 0

    existing_units = {} if force else load_existing_parameterized_units(output_path)
    parameterized_units: List[Dict[str, Any]] = []
    failures: List[str] = []

    for unit_index, concrete_unit in enumerate(units, start=1):
        unit_id = str(concrete_unit.get("unit_id", "")).strip() or f"{segment_id}_unit_{unit_index:02d}"
        debug_prefix = f"{segment_id}_{unit_id}"
        existing_unit = existing_units.get(unit_id)
        if existing_unit and is_reusable_done_parameterized_unit(existing_unit, concrete_unit):
            parameterized_units.append(existing_unit)
            log_with_timestamp(f"{debug_prefix}: reuse existing successful parameterization")
            continue

        success, parameterized_unit, error = generate_validated_parameterized_unit(
            segment_data=segment_data,
            source_path=source_path,
            concrete_unit=concrete_unit,
            session_dir=session_dir,
            debug_dir=debug_dir,
            debug_prefix=debug_prefix,
            model=model,
            max_retries=max_retries,
            retry_delay=retry_delay,
        )
        parameterized_units.append(parameterized_unit)
        if not success:
            failures.append(f"{unit_id}: {error}")

    segment_error = "; ".join(failures)
    payload = build_output_payload(
        segment_data=segment_data,
        parameterized_units=parameterized_units,
        phase2_error=segment_error,
    )
    output_path.write_text(
        json.dumps(payload, ensure_ascii=False, indent=2),
        encoding="utf-8",
    )

    unit_success_count = sum(1 for unit in parameterized_units if parameterized_unit_success(unit))
    unit_failure_count = len(parameterized_units) - unit_success_count

    if not failures:
        log_with_timestamp(
            f"{segment_id}: finished successfully in "
            f"{format_duration(time.perf_counter() - segment_perf_start)} "
            f"(started {format_timestamp(segment_wall_start)})"
        )
        return True, "", unit_success_count, unit_failure_count

    log_with_timestamp(
        f"{segment_id}: failed in {format_duration(time.perf_counter() - segment_perf_start)} "
        f"(started {format_timestamp(segment_wall_start)})"
    )
    return False, segment_error, unit_success_count, unit_failure_count


def resolve_default_model(explicit_model: str) -> str:
    if explicit_model:
        return explicit_model
    return "gpt-5.5"


def process_session(
    session_dir: Path,
    segments_units_dir: Path,
    output_dir: Path,
    status_path: Optional[Path],
    model: str,
    max_retries: int,
    retry_delay: float,
    segment_limit: int,
    force: bool,
) -> Tuple[bool, List[Tuple[str, str]], int, int, int]:
    debug_dir = output_dir / "_debug"
    output_dir.mkdir(parents=True, exist_ok=True)
    debug_dir.mkdir(parents=True, exist_ok=True)

    segment_files = list(iter_segment_files(segments_units_dir))
    if not segment_files:
        raise FileNotFoundError(f"No JSON files found under {segments_units_dir}")
    if segment_limit > 0:
        segment_files = segment_files[:segment_limit]

    log_with_timestamp(
        f"Session start: session={session_dir.name}, model={model}, "
        f"segments={len(segment_files)}, output_dir={output_dir}"
    )

    failures: List[Tuple[str, str]] = []
    total_units = 0
    total_unit_failures = 0
    completed_segments = 0
    skipped_segments = 0
    failed_segments = 0
    total_segments = len(segment_files)
    for file_index, source_path in enumerate(segment_files, start=1):
        segment_data = read_json_file(source_path)
        segment_id = str(segment_data.get("segment_id") or source_path.stem)
        output_path = output_dir / source_path.name
        if not force and segment_file_success(read_json_file(output_path)):
            parameterized_units = read_json_file(output_path).get("parameterized units") or []
            unit_success_count = sum(
                1 for unit in parameterized_units if isinstance(unit, dict) and parameterized_unit_success(unit)
            )
            total_units += unit_success_count
            completed_segments += 1
            skipped_segments += 1
            log_with_timestamp(
                f"{session_dir.name} [{file_index}/{total_segments}]: skip {source_path.name} -> {segment_id} "
                f"(completed output exists)"
            )
            if status_path is not None:
                status_data = read_json_file(status_path)
                status_data.update(
                    {
                        "current_segment_index": file_index,
                        "current_segment_id": segment_id,
                        "current_source": str(source_path),
                        "segments_total": total_segments,
                        "segments_completed_so_far": completed_segments,
                        "segments_skipped_so_far": skipped_segments,
                        "segments_failed_so_far": failed_segments,
                        "units_processed_so_far": total_units,
                        "units_failed_so_far": total_unit_failures,
                        "last_progress_at": format_timestamp(datetime.now()),
                    }
                )
                write_status_file(status_path, status_data)
            continue

        log_with_timestamp(
            f"{session_dir.name} [{file_index}/{total_segments}]: processing {source_path.name} -> {segment_id}"
        )
        if status_path is not None:
            status_data = read_json_file(status_path)
            status_data.update(
                {
                    "current_segment_index": file_index,
                    "current_segment_id": segment_id,
                    "current_source": str(source_path),
                    "segments_total": total_segments,
                    "segments_completed_so_far": completed_segments,
                    "segments_skipped_so_far": skipped_segments,
                    "segments_failed_so_far": failed_segments,
                    "units_processed_so_far": total_units,
                    "units_failed_so_far": total_unit_failures,
                    "last_progress_at": format_timestamp(datetime.now()),
                }
            )
            write_status_file(status_path, status_data)

        ok, error, unit_success_count, unit_failure_count = process_segment_file(
            source_path=source_path,
            session_dir=session_dir,
            output_dir=output_dir,
            debug_dir=debug_dir,
            model=model,
            max_retries=max_retries,
            retry_delay=retry_delay,
            force=force,
        )
        total_units += unit_success_count + unit_failure_count
        total_unit_failures += unit_failure_count
        if not ok:
            failed_segments += 1
            failures.append((segment_id, error))
        else:
            completed_segments += 1

        if status_path is not None:
            status_data = read_json_file(status_path)
            status_data.update(
                {
                    "current_segment_index": file_index,
                    "current_segment_id": segment_id,
                    "current_source": str(source_path),
                    "segments_total": total_segments,
                    "segments_completed_so_far": completed_segments,
                    "segments_skipped_so_far": skipped_segments,
                    "segments_failed_so_far": failed_segments,
                    "units_processed_so_far": total_units,
                    "units_failed_so_far": total_unit_failures,
                    "last_progress_at": format_timestamp(datetime.now()),
                }
            )
            write_status_file(status_path, status_data)

    return not failures, failures, len(segment_files), total_units, total_unit_failures


def main() -> int:
    parser = argparse.ArgumentParser(
        description="Phase-2 unit abstraction and parameterization for one session or a batch of sessions.",
    )
    parser.add_argument(
        "input_path",
        help="Session directory path, unit JSON directory path, or a batch root containing multiple session directories.",
    )
    parser.add_argument(
        "--units-subdir",
        default="segments_units_denoised",
        help="Relative subdirectory under each session that contains Phase-1 unit JSON files. Default: segments_units_denoised. Example: segments_units",
    )
    parser.add_argument(
        "--output-dir",
        default="",
        help="Directory for generated outputs. Single-session mode writes directly here; batch mode writes to <output-dir>/<session_name>/segments_units_phase2. Defaults to <session>/segments_units_phase2.",
    )
    parser.add_argument(
        "--model",
        default="gpt-5.5",
        help="Codex model name. Default: gpt-5.5.",
    )
    parser.add_argument(
        "--max-retries",
        type=int,
        default=3,
        help="Maximum generation retries per unit. Default: 3.",
    )
    parser.add_argument(
        "--retry-delay",
        type=float,
        default=2.0,
        help="Seconds to wait between retries. Default: 2.0.",
    )
    parser.add_argument(
        "--limit",
        type=int,
        default=0,
        help="Optional maximum number of segment JSON files to process per trajectory. 0 means no limit.",
    )
    parser.add_argument(
        "--trajectory-limit",
        type=int,
        default=0,
        help="Optional maximum number of trajectory directories to process in batch mode. 0 means no limit.",
    )
    parser.add_argument(
        "--batch",
        action="store_true",
        help="Treat input_path as a batch root and process trajectory directories under it.",
    )
    parser.add_argument(
        "--recursive",
        action="store_true",
        help="When --batch is used, recursively search for trajectory directories. Without it, only direct children are scanned.",
    )
    parser.add_argument(
        "--stop-on-error",
        action="store_true",
        help="Stop immediately when one trajectory fails. By default, batch mode continues with the next trajectory.",
    )
    parser.add_argument(
        "--log-file",
        default="",
        help="Optional file path for timestamped runtime logs. Console output is still printed.",
    )
    parser.add_argument(
        "--force",
        action="store_true",
        help="Reprocess trajectories and segments even if successful Phase-2 outputs already exist.",
    )
    args = parser.parse_args()
    run_wall_start = datetime.now()
    run_perf_start = time.perf_counter()

    script_path = Path(__file__).resolve()
    for candidate in (
        script_path.parent / ".env",
        script_path.parents[1] / ".env",
        script_path.parents[2] / ".env",
        script_path.parents[3] / ".env",
    ):
        load_env_file(candidate)
    configure_log_file(args.log_file)

    input_path = Path(args.input_path).resolve()
    units_subdir = normalize_units_subdir(args.units_subdir)
    if args.batch:
        session_dirs = discover_batch_session_dirs(
            input_path=input_path,
            units_subdir=units_subdir,
            recursive=args.recursive,
        )
        batch_mode = True
    else:
        session_dirs = discover_session_dirs(input_path, units_subdir)
        batch_mode = len(session_dirs) > 1 or (
            input_path.is_dir() and input_path not in session_dirs
        )
    if args.trajectory_limit > 0:
        session_dirs = session_dirs[: args.trajectory_limit]

    model = resolve_default_model(args.model)

    log_with_timestamp(
        f"Run start: model={model}, trajectories={len(session_dirs)}, "
        f"batch_mode={batch_mode}, recursive={args.recursive}, units_subdir={units_subdir.as_posix()}"
    )
    if LOG_FILE_PATH is not None:
        log_with_timestamp(f"Runtime log file: {LOG_FILE_PATH}")

    trajectory_failures: List[Tuple[str, str]] = []
    processed_sessions = 0
    skipped_sessions = 0

    for session_index, session_dir in enumerate(session_dirs, start=1):
        _, segments_units_dir = resolve_session_paths(session_dir, units_subdir)
        output_dir = resolve_output_dir_for_session(
            session_dir=session_dir,
            explicit_output_dir=args.output_dir,
            batch_mode=batch_mode,
        )
        status_path = status_file_path_for_session(session_dir)
        status_data = read_json_file(status_path)
        previous_status = str(status_data.get("status", "")).strip().lower()

        outputs_are_complete = session_outputs_completed(
            segments_units_dir=segments_units_dir,
            output_dir=output_dir,
            segment_limit=args.limit,
        )

        if previous_status == "done" and outputs_are_complete and not args.force:
            skipped_sessions += 1
            log_with_timestamp(
                f"[{session_index}/{len(session_dirs)}] skip {session_dir.name}: already marked done at "
                f"{status_data.get('finished_at', 'unknown time')}"
            )
            continue
        if previous_status == "done" and not outputs_are_complete and not args.force:
            log_with_timestamp(
                f"[{session_index}/{len(session_dirs)}] resume {session_dir.name}: status is done but "
                f"some Phase-2 outputs are missing or invalid"
            )

        session_wall_start = datetime.now()
        session_perf_start = time.perf_counter()
        write_status_file(
            status_path,
            {
                "status": "in_progress",
                "session_dir": str(session_dir),
                "segments_units_dir": str(segments_units_dir),
                "units_subdir": units_subdir.as_posix(),
                "output_dir": str(output_dir),
                "model": model,
                "started_at": format_timestamp(session_wall_start),
                "finished_at": "",
                "duration_seconds": 0.0,
                "segments_total": 0,
                "segments_succeeded": 0,
                "segments_failed": 0,
                "units_total": 0,
                "units_succeeded": 0,
                "units_failed": 0,
                "failed_segments": [],
            },
        )

        log_with_timestamp(
            f"[{session_index}/{len(session_dirs)}] processing trajectory {session_dir.name}"
        )

        ok = False
        failures: List[Tuple[str, str]] = []
        segment_count = 0
        unit_total = 0
        unit_failed = 0
        error_message = ""
        try:
            ok, failures, segment_count, unit_total, unit_failed = process_session(
                session_dir=session_dir,
                segments_units_dir=segments_units_dir,
                output_dir=output_dir,
                status_path=status_path,
                model=model,
                max_retries=args.max_retries,
                retry_delay=args.retry_delay,
                segment_limit=args.limit,
                force=args.force,
            )
        except Exception as exc:
            error_message = f"{type(exc).__name__}: {exc}"
            failures = [("session", error_message)]
            ok = False

        duration_seconds = time.perf_counter() - session_perf_start
        segment_failed_count = sum(
            1 for segment_id, _ in failures if segment_id != "session"
        )
        segment_success_count = max(0, segment_count - segment_failed_count)
        unit_success_count = max(0, unit_total - unit_failed)
        write_status_file(
            status_path,
            {
                "status": "done" if ok else "failed",
                "session_dir": str(session_dir),
                "segments_units_dir": str(segments_units_dir),
                "units_subdir": units_subdir.as_posix(),
                "output_dir": str(output_dir),
                "model": model,
                "started_at": format_timestamp(session_wall_start),
                "finished_at": format_timestamp(datetime.now()),
                "duration_seconds": round(duration_seconds, 3),
                "segments_total": segment_count,
                "segments_succeeded": segment_success_count,
                "segments_failed": segment_failed_count,
                "units_total": unit_total,
                "units_succeeded": unit_success_count,
                "units_failed": unit_failed,
                "failed_segments": [
                    {"segment_id": segment_id, "error": error}
                    for segment_id, error in failures
                ],
            },
        )

        processed_sessions += 1
        if ok:
            log_with_timestamp(
                f"Trajectory {session_dir.name} finished successfully in {format_duration(duration_seconds)}"
            )
        else:
            trajectory_failures.append(
                (session_dir.name, error_message or "; ".join(f"{sid}: {err}" for sid, err in failures))
            )
            log_with_timestamp(
                f"Trajectory {session_dir.name} failed in {format_duration(duration_seconds)}"
            )
            if args.stop_on_error:
                log_with_timestamp("Stop on error is enabled; aborting remaining trajectories.")
                break

    log_with_timestamp(
        f"Run finished in {format_duration(time.perf_counter() - run_perf_start)} "
        f"(started {format_timestamp(run_wall_start)})"
    )
    print(
        f"\nProcessed trajectories: {processed_sessions}, skipped: {skipped_sessions}, "
        f"failed: {len(trajectory_failures)}"
    )
    if batch_mode:
        print(f"Batch root: {input_path}")
    else:
        print(f"Session root: {session_dirs[0]}")

    if args.output_dir:
        print(f"Output base: {Path(args.output_dir).resolve()}")
    else:
        print("Output base: each session directory / segments_units_phase2")

    if trajectory_failures:
        print("Failed trajectories:")
        for session_name, error in trajectory_failures:
            print(f"  - {session_name}: {error}")
        return 1

    print("All requested trajectories finished successfully.")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
